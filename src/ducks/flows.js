const initialState = {
  'develop': { name: 'Разработка' },
  'management': { name: 'Управление' },
  'admin': { name: 'Администрирование' }
}

export default (state = initialState, action) => {
  return state
}
